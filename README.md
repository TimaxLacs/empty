# Комплектация одежды, экипировки и снаряжения

Сводка по предметам: у кого есть, у кого нет, у кого едет.

Главный файл для копирования: [equipment/SVODKA.txt](equipment/SVODKA.txt).

Знаки в данных: `+` есть, `-` нет, `>` едет, `~` частично, `!` есть/взять, `x` не требуется, `?` не сказано.
